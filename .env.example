# Meta Pixel (Facebook Pixel) dataset ID for "CBoom Web".
# Not a secret — this ID is publicly visible in every page's client-side script
# — but it's kept as an env var so staging/preview builds can omit or swap it.
# Leave unset to disable the Meta Pixel entirely (e.g. local development).
PUBLIC_META_PIXEL_ID=1652154163003052
